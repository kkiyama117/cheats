% installer, zsh, completions

# create completions for rustup 
rustup completions zsh > $ZDOTDIR/completions/_rustup
rustup completions zsh cargo > $ZDOTDIR/completions/_cargo

# create completions for deno
deno completions zsh > $ZDOTDIR/completions/_deno

# create completions for rhq
rhq completion zsh $ZDOTDIR/completions/_rhq

# create completions for pueue
pueue completions zsh $ZDOTDIR/completions

